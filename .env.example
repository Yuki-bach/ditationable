# Copy this file to .env.local and add your configuration

# No server-side API keys are stored in this application
# API keys are provided by users and stored only in browser memory

# Optional: Add any future configuration here
NODE_ENV=development